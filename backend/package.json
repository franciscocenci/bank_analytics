{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "dev": "docker-compose up db -d && npm run wait-postgres && if [ -z \"$SKIP_MIGRATIONS\" ]; then npm run db:migrate; fi && npm run start",
    "start": "node src/server.js",
    "down": "docker-compose down",
    "reset": "docker-compose down -v",
    "wait-postgres": "node src/infra/wait-postgres.js",
    "db:migrate": "sequelize db:migrate",
    "db:migrate:undo": "sequelize db:migrate:undo",
    "seed:admin": "node src/seeders/seedAdmin.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^7.5.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.13",
    "pg": "^8.17.2",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7",
    "wait-on": "^9.0.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "bcryptjs": "^3.0.3",
    "jest": "^29.7.0",
    "sequelize-cli": "^6.6.2"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "clearMocks": true
  }
}
